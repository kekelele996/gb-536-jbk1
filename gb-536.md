# gb-536 语料标注一致性与分歧裁决

请生成一个名为 `corpus-annotation-agreement-control` 的 Go 全栈 Web 项目，面向企业 NLP 数据团队，用于维护内部语料集与标注规范、接收多标注员结果、计算一致性并对分歧进行裁决。它是企业内部数据质量系统，不做大众众包社区、客服工单、商城、电商、预约、订单、仓储库存、财务或博客。

## 1. 项目概述与复杂度下限

- 类型：B2B 前后端分离全栈 Web 应用。
- 必须实现 4 个核心实体、5 个核心页面、至少 3 个共享组件、至少 2 个 hooks、至少 4 个后端中间件和 2 组前后端一致的共享枚举。
- 四实体贯穿 PostgreSQL 表、Go model/dto/repository/service/handler/router、Angular type/api/store/page；敏感原文不得写入日志。
- 本项目用于验证跨文件协同改动能力。严禁合并职责到单一文件；模式、枚举、状态、算法或权限变更应触达至少 5 个文件。

## 2. 核心实体

1. `CorpusDataset`（语料数据集）
   - 字段：`id`、`dataset_code`、`name`、`language`、`domain`、`document_count`、`content_mask_policy`、`dataset_state`、`version`、`owner_team`、`created_at`。
   - 关系与动作：拥有标注规范和多组标注结果；支持建立元数据、导入脱敏样例、冻结版本和归档。
   - 必须贯穿 `corpus_datasets` 表 -> `model/corpus_dataset.go` -> dto/repository/service/handler/router 独立文件 -> `frontend/src/types/corpus-dataset.ts`、api、store、数据集页面。
2. `AnnotationSchema`（标注规范）
   - 字段：`id`、`dataset_id`、`schema_code`、`version`、`label_definitions_json`、`span_policy`、`overlap_policy`、`examples_json`、`schema_state`、`created_by`、`published_at`。
   - 关系与动作：隶属数据集并约束标注结果；支持草拟、校验、发布、废止和复制版本。
   - 必须贯穿 `annotation_schemas` 表及独立后端分层、前端 type/api/store/page 文件。
3. `AnnotationSet`（标注结果集）
   - 字段：`id`、`dataset_id`、`schema_id`、`annotator_id`、`item_key`、`labels_json`、`source_checksum`、`annotation_state`、`submitted_at`、`supersedes_id`、`quality_note`。
   - 关系与动作：同一 item 至少关联两位标注员结果，并可被裁决引用；支持保存、提交、锁定、退回和被新版本替代。
   - 必须贯穿 `annotation_sets` 表及独立后端分层、前端 type/api/store/page 文件。
4. `AdjudicationCase`（分歧裁决）
   - 字段：`id`、`dataset_id`、`item_key`、`annotation_set_ids`、`agreement_metric`、`agreement_score`、`disagreement_type`、`confusion_snapshot_json`、`cluster_key`、`case_state`、`final_labels_json`、`rationale`、`adjudicator_id`、`decided_at`。
   - 关系与动作：冻结参与比较的结果集；支持生成、分组、认领、裁决、复核和重开，保留全部历史。
   - 必须贯穿 `adjudication_cases` 表及独立后端分层、前端 type/api/store/page 文件。

## 3. 差异化算法与状态流

- 对分类任务计算 Cohen's Kappa；多标注员或含缺失值场景计算 Krippendorff's Alpha。必须显示观测一致率、机会一致率、样本量与适用条件，不能只显示单一分数。
- 对 span 标注先按字符区间做精确/部分重叠匹配，再区分边界、标签、遗漏、重叠分歧；生成标签混淆矩阵，并按 `schema + disagreement_type + label_pair` 聚类待裁决项。
- `AnnotationSet`：`draft -> submitted -> locked -> compared`，`submitted -> returned -> draft`，`compared -> superseded`；非法迁移返回 409。
- `AdjudicationCase`：`open -> assigned -> adjudicated -> reviewed -> accepted`，`reviewed -> reopened`；裁决员不得处理自己的标注结果。
- 统计实现必须有已知小样本夹具与边界测试。系统是内部质量辅助工具，不对外众包或形成大众社区。

## 4. 核心页面与实体消费

1. `/datasets` 数据集：消费 `CorpusDataset + AnnotationSchema`，查看版本、语言、数量和脱敏策略。
2. `/schemas` 规范编辑：消费 `AnnotationSchema + CorpusDataset`，维护标签、span/overlap 规则和版本。
3. `/annotations` 标注比较：消费 `AnnotationSet + AnnotationSchema + CorpusDataset`，并排展示多个结果和差异高亮。
4. `/adjudication` 分歧裁决：消费 `AdjudicationCase + AnnotationSet + AnnotationSchema`，查看指标、混淆矩阵、聚类和裁决状态。
5. `/audit` 审计中心：消费四实体变更投影与脱敏审计记录，按 request ID、实体、操作者和时间筛选。

`AnnotationStateBadge` 被标注和裁决页共用；`AgreementMatrix` 被数据集和裁决页共用；`DiffEvidenceDrawer` 被标注、裁决和审计页共用。

## 5. 横切关注点

- JWT + RBAC：角色至少为 `admin`、`data_manager`、`annotator`、`adjudicator`、`auditor`；联动角色表、JWT、Go auth/rbac、路由、Angular guard、按钮显隐和类型。annotator 只能改自己的草稿，adjudicator 不能裁决自己的结果。
- 审计、脱敏与追踪：记录操作者、request ID、实体、动作和结构化差异，但不得记录语料原文或完整 labels 内容；统计运行保存输入集合哈希和算法版本。
- 全局错误：统一错误码；规范不兼容/标注无效返回 422，状态和版本冲突返回 409，前端统一处理。
- 限流与幂等：登录、标注提交和一致性计算接口限流；计算和裁决提交使用 `Idempotency-Key`。

## 6. 共享枚举、组件、hooks 与中间件

- `AnnotationState = draft | submitted | returned | locked | compared | superseded`：后端 `backend/internal/constants/annotation_state.go`，前端 `frontend/src/types/enums/annotation-state.ts`。
- `DisagreementType = boundary | label | omission | overlap`：后端 `backend/internal/constants/disagreement_type.go`，前端 `frontend/src/types/enums/disagreement-type.ts`。
- README 列出枚举在 model、dto、算法、service 状态机、store、组件、页面和测试中的所有出现位置。
- 共享组件：`AnnotationStateBadge`、`AgreementMatrix`、`DiffEvidenceDrawer`；Angular inject helpers：`useAuth`、`useAdjudicationQueue`，位于 `frontend/src/hooks/`。
- 至少独立实现 `request_id.go`、`recovery.go`、`auth.go`、`rbac.go`、`audit.go`、`error_handler.go`；另提供脱敏 logger util。

## 7. 技术、规模与文件结构

- 英文短名：`corpus-annotation-agreement-control`。
- 前端：Angular 17 + TypeScript + Vite + Angular Material + Signals store；后端：Go 1.22 + Gin + GORM；正式 PostgreSQL 16，runtime smoke 支持 SQLite；JWT。
- API 统一使用 `/api/v1`，另提供真实 `/healthz`；使用 validator/v10、统一响应和错误码、`%w` 错误链、request ID、结构化日志、构造器注入并实现优雅停机。多表写入用事务，状态迁移用条件更新，唯一性/幂等不能只靠前端。禁止 TODO、placeholder、空函数、硬编码成功响应和 handler 直连数据库。
- Go 功能代码 2500-4200 行、24-42 个功能 `.go` 文件且少于 5000 行；不计 `_test.go`、生成代码和 vendor。一致性公式、span 匹配、状态机、脱敏和权限须有测试。
- 只保留普通项目单测；不得创建名称含 record/regression/grading/scoring 的评分测试，不得加入答案、`BUG_REPRO.md` 或标注流水线内部文件。
- 根 `go.work` 纳入 `./backend`，支持根执行 `go work sync`、`go build ./backend/...`、`go test ./backend/...`。

```text
.
├── go.work  runtime_smoke.json
├── docker-compose.yml  .env  .env.example  .gitignore  README.md
├── output/
│   └── execution.md
├── backend/
│   ├── go.mod  Dockerfile  cmd/server/main.go
│   └── internal/
│       ├── config/ constants/ dto/ model/
│       ├── repository/ service/ handler/ router/
│       ├── middleware/ algorithm/ matching/ util/
├── frontend/
│   ├── package.json  Dockerfile  nginx.conf
│   └── src/
│       ├── api/ stores/ types/ components/common/
│       ├── hooks/ pages/ router/ utils/
└── database/init.sql
```

四实体必须在所有后端层与前端 api/store/type 中按实体分文件。严禁合并职责到单一文件，不得以通用 CRUD handler/store 替代实体模块。

## 8. 运行 Manifest、部署与验证交付

- 固定端口：前端 `18536`、后端 `19536`、PostgreSQL 宿主机 `57536`；容器内 `80/8080/5432`。
- 根 `runtime_smoke.json` 的唯一用途是提供可解析的启动 manifest，内容必须严格等价于：

```json
{
  "mode": "service",
  "start": "go run ./cmd/server",
  "workdir": "backend",
  "env": {
    "PORT": "20536",
    "DB_DRIVER": "sqlite",
    "DB_DSN": "file:runtime-smoke?mode=memory&cache=shared",
    "JWT_SECRET": "runtime-smoke-only-change-me"
  },
  "ready_url": "http://127.0.0.1:20536/healthz",
  "ready_status": [200],
  "startup_timeout": 30
}
```

- 后端必须支持 SQLite smoke 配置并自动建表/种子数据；正式 Compose 仍使用 PostgreSQL。manifest 禁止出现 `passed`、截图或执行证据字段。
- Compose 顶层 `name: corpus-annotation-agreement-control` 且无 `version:`；编排 db/backend/frontend，使用命名卷和英文短名容器名前缀，数据库、后端和前端均有 healthcheck，并按健康条件依赖。
- `.env`、`.env.example` 均含 `COMPOSE_PROJECT_NAME=corpus-annotation-agreement-control`、`FRONTEND_PORT=18536`、`BACKEND_PORT=19536`、`DB_PORT=57536`、`DB_DRIVER=postgres`、DB 变量和 `JWT_SECRET`；`.env` 不提交。前端只访问 `/api`，Nginx 原样代理 `/api/v1` 到 `backend:8080`，另提供 `/api/healthz` 并支持 SPA。
- README 首屏给 `docker compose up -d`，说明项目介绍、主要功能、角色账号、架构与目录、API 清单、技术栈表、环境变量与端口、本地开发、Docker 部署、枚举位置、算法适用条件与脱敏边界、runtime smoke、构建测试、常见问题、停止方式和 License；README 不得承载执行证据。
- 真实执行 `go work sync`、`go build ./backend/...`、`go vet ./backend/...`、`go test ./backend/...`、`npm --prefix frontend ci`、`npm --prefix frontend run build`、`docker compose config --quiet`、`docker compose up -d --build`、`docker compose ps`；前后端均须有 healthcheck，所有长驻服务必须 healthy。
- 至少 8 个真实接口冒烟：health、登录、四实体查询、数据集/规范创建、标注提交、合法/非法状态迁移、一致性计算、裁决读取、403 权限；覆盖成功和失败分支。
- API 冒烟还必须包含详情读取、未认证 401 与越权 403；四个核心实体各至少经过一条真实后端链路，五个页面都消费真实 `/api/v1`，不得使用静态 mock。并在 `backend/` 内复跑 `go build ./...`、`go vet ./...`、`go test ./...`。
- 仅用 Codex 内置 Browser 验证登录、数据集、规范、标注比较、裁决五页，禁止外部 Chrome；实际完成创建或编辑、列表刷新和一次状态变化，并检查 console/network、文字溢出和遮挡。
- 所有构建、测试、runtime smoke、Compose、至少 8 条 API、Browser、console/network 和关闭证据只能写入 `output/execution.md`；截图必须直接放 `output/`，至少桌面和移动端各一张并由 execution.md 引用；不得记录敏感原文，不得在其他文件复制证据或伪造结果。
- `output/execution.md` 必须包含项目编号、验证时间、实际端口、规模实测、根与 backend 构建/vet/test、前端构建、服务 healthy、API 表、Browser 操作步骤、截图相对路径、逐条需求覆盖、修复摘要、停服无残留和实现提交 hash。
- 验证后执行 `docker compose down -v --remove-orphans`，确认容器、网络和命名卷均已清理，并停止 runtime smoke 进程。
- 项目必须独立 `git init`，设置本地身份 `git config user.name "blueship581"`、`git config user.email "brysj.hhrhl.g@gmail.com"`。真实实现与验证完成后提交 `git add -A && git commit -m "feat: implement and verify corpus-annotation-agreement-control"`，把实现提交 hash 写入 `output/execution.md`，再执行 `git add output/execution.md && git commit -m "docs: record verified execution"`，形成两次提交。`.gitignore` 排除 `.env`、依赖、构建产物、日志和本地 SQLite 文件，但保留 execution.md 与截图；最终 `git status --short` 为空，不得共用 Git 仓库。
